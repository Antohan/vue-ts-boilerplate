# Contains the application domain models

### Example:
```(ts)
export default class ModelName {
    // Class fields
    field: string;

    constructor(data: ClassName) {
        this.field = data.field;        
    }
}
```
